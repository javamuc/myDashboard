import { Route } from '@angular/router';

import { AccountLockedComponent } from './account-locked.component';

export const accountLockedRoute: Route = {
  path: 'account-locked',
  component: AccountLockedComponent,
  title: 'Account Locked',
};
